@model IEnumerable<Vishop.Models.Movie>

@{
    ViewBag.Title = "AllMovies";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>All movies</h2>

@if(!Model.Any())
{
    <text>No movies yet.</text>
} else
{
    @Html.ActionLink("Add new","New","Movie",new { },new { @class="btn btn-primary"})
    <table class="table table-striped table-­bordered table‐hover">
        <thead>
            <tr>
                <th>Movie Name</th>
                <th>Genre</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var movie in Model)
            {
                <tr>
                    <td>@Html.ActionLink(movie.name, "Edit", "Movie", new { id = movie.id }, null)</td>
                    <td>@movie.Genre.name</td>
                </tr>
            }
            </tbody>
        </table>
}